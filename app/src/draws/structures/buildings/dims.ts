// Shared building body dimensions — one source for the variant draws + the
// Building (HP-bar / scaffold placement). A tiny module avoids a Building↔registry
// import cycle (mirrors draws/towers/dims).
export const BUILDING_HEIGHT = 40;
export const BUILDING_HALF_W = 16;
